class Solution {
    public int changeOne(String startGene, String endGene, String[] bank, int loc) {
        if (startGene.charAt(loc) != )
        String testGene = startGene.substring(0, loc) + endGene.charAt(loc) + startGene.substring(loc+1, startGene.length());

        for (int i = 0; i < bank.lenght; i++) {

        }
    }

    public int minMutation(String startGene, String endGene, String[] bank) {
        
    }
}