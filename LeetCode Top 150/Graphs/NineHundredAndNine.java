public class NineHundredAndNine {
    public int snakesAndLadders(int[][] board) {
        int endpoint = board.length*board[0].length;
        int currLocation = 1;
        int moveCounter = 0;

        while (currLocation < endpoint) {
            int farthestMove = 0;

            for (int i = 1; i < 7; i++) {
                int currentMove = board[board.length]
            }
        }

        return 1;
    }
}
